price$DECIMAL(11,2)$商品价格
has_post_fee$int$是否包含邮费
adjust_fee$DECIMAL(11,2)$调整费用
received_payment$DECIMAL(11,2)$支付宝实际收款金额
order_id$VARCHAR(36)$订单号
tid$VARCHAR(36)$来源单号
buyer_nick$VARCHAR(36)$买家昵称
receiver_name$VARCHAR(36)$收货人姓名
receiver_mobile$VARCHAR(36)$收货电话
zip_code$VARCHAR(36)$邮编
receiver_address$VARCHAR(36)$收货地址
created_time$DATETIME$下单时间
pay_time$DATETIME$付款时间
collect_time$DATETIME$系统接收时间
pickup_DATETIME$DATETIME$提货日期
arrival_DATETIME$DATETIME$到货日期
status$$订单状态
type$$订单类型(交易类型列表，同时查询多种交易类型可用逗号分隔。默认同时查询guarantee_trade, auto_delivery, ec, cod的4种交易类型的数据 可选值 fixed(一口价) auction(拍卖) guarantee_trade(一口价、拍卖) auto_delivery(自动发货) independent_simple_trade(旺店入门版交易) independent_shop_trade(旺店标准版交易) ec(直冲) cod(货到付款) fenxiao(分销) game_equipment(游戏装备) shopex_trade(ShopEX交易) netcn_trade(万网交易) external_trade(统一外部交易)o2o_offlinetrade（O2O交易）step (万人团)nopaid(无付款订单)pre_auth_type(预授权0元购机交易))
shop_id$VARCHAR(36)$店铺ID
platform$VARCHAR(36)$平台
warehouse$VARCHAR(36)$仓库
customer_id$VARCHAR(36)$客户编码
customer_name$VARCHAR(36)$客户名称
discount_info$VARCHAR(36)$优惠信息
payment$DECIMAL(11,2)$订单总金额(子订单实付金额。精确到2位小数，单位:元。如:200.07，表示:200元7分。对于多子订单的交易，计算公式如下：payment = price * num + adjust_fee - discount_fee ；单子订单交易，payment与主订单的payment一致，对于退款成功的子订单，由于主订单的优惠分摊金额，会造成该字段可能不为0.00元。建议使用退款前的实付金额减去退款单中的实际退款金额计算。)
total_fee$DECIMAL(11,2)$实付金额(应付金额（商品价格 * 商品数量 + 手工调整金额 - 子订单级订单优惠金额）。精确到2位小数;单位:元。如:200.07，表示:200元7分)
wx_pay$DECIMAL(11,2)$微信支付
total_over$DECIMAL(11,2)$余额总额
yecz_pay$DECIMAL(11,2)$余额充值部分支付
yezs_pay$DECIMAL(11,2)$余额赠送部分支付
yelpk_pay$DECIMAL(11,2)$余额礼品卡部分支付
credit_card_fee$DECIMAL(11,2)$银行卡支付
lwdhj_pay$DECIMAL(11,2)$礼物兑换券支付
xj_pay_amount$DECIMAL(11,2)$现金支付金额
wx_refund_amount$DECIMAL(11,2)$微信退款金额
zfb_refund_amount$DECIMAL(11,2)$支付宝退款金
yue_refund_amount$DECIMAL(11,2)$余额退款金额
discount_fee$DECIMAL(11,2)$优惠金额
receivable_fee$DECIMAL(11,2)$应收快递费
post_fee$DECIMAL(11,2)$实收快递费
logistics_company$VARCHAR(36)$物流公司(字段来自子订单)
invoice_no$VARCHAR(36)$物流单号(字段来自子订单)
consign_time$DATETIME$发货时间
sign_time$DATETIME$签收时间
suit_num$int$套装数
single_num$int$单品数
single_total_num$int$单品合计数
gift_num$int$赠品数
buyer_message$VARCHAR(36)$买家留言
seller_flag$int$卖家备注旗帜（与淘宝网上订单的卖家备注旗帜对应，只有卖家才能查看该字段）红、黄、绿、蓝、紫 分别对应 1、2、3、4、5
customer_service_memo$VARCHAR(36)$客服备注
logistics_memo$VARCHAR(36)$物流备注
warehousing_memo$VARCHAR(36)$仓储备注
other_num$VARCHAR(36)$其他备注
province$VARCHAR(36)$省
city$VARCHAR(36)$市
area$VARCHAR(36)$区(收货人的所在地区注：因为国家对于城市和地区的划分的有：省直辖市和省直辖县级行政区（区级别的）划分的，淘宝这边根据这个差异保存在不同字段里面比如：广东广州：广州属于一个直辖市是放在的receiver_city的字段里面；而河南济源：济源属于省直辖县级行政区划分，是区级别的，放在了receiver_district里面建议：程序依赖于城市字段做物流等判断的操作，最好加一个判断逻辑：如果返回值里面只有receiver_district参数，该参数作为城市)
create_name$VARCHAR(36)$创单人
split_flag$INT$拆分标识
split_before_orderId$VARCHAR(36)$拆分前订单号
Consign_id$VARCHAR(36)$发货单编号
num$int$商品数量
trade_from$VARCHAR(36)$交易内部来源。(WAP(手机);HITAO(嗨淘);TOP(TOP平台);TAOBAO(普通淘宝);JHS(聚划算)一笔订单可能同时有以上多个标记，则以逗号分隔)